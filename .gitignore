# Python
__pycache__/
*.py[cod]
*.so

# Packaging / build artifacts
build/
dist/
*.egg-info/
src/*.egg-info/

# Virtual environments
venv/
.venv/
env/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store

# Microsoft Office lock/owner files (e.g. ~$Presentation.pptx)
~$*

# Claude local settings
.claude/settings.local.json

# Raw per-listing image dump (regenerated via strat-prepare / scraping.download)
data/images/

# NOTE: the labeled dataset (data/images_labeled/) and listing metadata
# (data/json/, data/strats.csv) ARE committed so the notebooks are runnable
# on a fresh clone.
